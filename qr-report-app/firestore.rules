rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary open rules for initial setup
    // IMPORTANT: Replace these with the secure rules after creating your admin account
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}